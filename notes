TODO
- ver si puede estar mal, o si necesitan randf
	- 'put_pixel(image, pixels[(int) ((i - WINDOW_SIZE / 2 + size / 2) * IMAGESIZE / size)], i, n);'
	- texture[(int) (IMAGESIZE * colision)] 

NOTES
- colors in input little endian (al reves)
- colors in output big endian
- ahora mismo, los rayos y el tamaño de la pantalla tienen que coincidir